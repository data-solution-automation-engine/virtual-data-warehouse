--
-- Presentation table creation statement for {{generationSpecificMetadata.selectedDataObject.name}}
--

DROP TABLE IF EXISTS [{{generationSpecificMetadata.selectedDataObject.name}}]

CREATE TABLE [{{lookupExtension generationSpecificMetadata.selectedDataObject.extensions "datastore"}}].[{{lookupExtension generationSpecificMetadata.selectedDataObject.extensions "location"}}].[{{generationSpecificMetadata.selectedDataObject.name}}]
(
   {{#each generationSpecificMetadata.selectedDataObject.dataItems}}
   [{{name}}] {{dataType}}{{#stringcompare dataType int}}{{else}}({{characterLength}}{{numericScale}}){{/stringcompare}},
   {{/each}}
   [{{conventions.eventDateTimeAttribute}}] DATETIME2(7),
   [{{conventions.changeDataCaptureAttribute}}] CHAR(1),
   [{{conventions.etlProcessAttribute}}] INT,
   [{{conventions.sourceRowIdAttribute}}] INT,
   [{{conventions.recordChecksumAttribute}}] BINARY(16)
)
